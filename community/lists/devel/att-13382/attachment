<p dir="ltr"></p>
<p dir="ltr">On Dec 4, 2013 12:07 PM, &quot;Jeff Squyres (jsquyres)&quot; &lt;<a href="mailto:jsquyres@cisco.com">jsquyres@cisco.com</a>&gt; wrote:<br>
[...] <br>
&gt; But in some ways, having uncompilable code is a *good* thing, because it tells you exactly where you need to work on the architecture.  Just updating it to *compile* removes that safeguard -- will you remember/re-find all those places where it *used* to block and convert the architecture to workaround the blocking?</p>

<p dir="ltr">+1</p>
<p dir="ltr">IMHO code that doesn&#39;t compile is much better than code that compiles but is known to be broken.</p>
<p dir="ltr">Adding a grepable &quot;FIXME&quot; sort of comment to each of these known broken code blocks would seem to be a decent compromise.</p>
<p dir="ltr">-Paul [Sent from my phone] <br>
</p>

