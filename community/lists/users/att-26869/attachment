<div dir="ltr"><div><div>Hi Team,<br><br></div>We are developing an application which can handle only POSIX threads and POSIX synchronization APIs.   I would like to know if the OpenMPI runtime implementation makes use of the POSIX
threads and POSIX synchronization APIs (lock, mutex, semaphore, signal, etc.)
or not,  in particular, the communication/buffer management layer. <br><br>

<p class="MsoNormal"><br></p>Regards,<br></div>Chaitra<br><div>

<p class="MsoNormal"> </p>

<br></div></div>

